version: '3.8'

services:
  discord-bot:
    build: .
    container_name: discord-economy-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./database:/app/database
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
